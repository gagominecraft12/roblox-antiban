export var ban = document.querySelector("fieldset");
export var msgChild = document.querySelector("p");
export var removeBan = ban.style.display = "none";
export var msg = 0x002();this.length=50,this.apply(this.arguments(function(){}, children()))
throw new Error() = (
    0x0003 = "valerr =", removeBan.includes("index", "statement", 0, 1, 0x0001)
)
if (navigator.webdriver("InternetExplorerDriver")) {
    throw new Error() = (
        window.location.href = "http://roblox.com/unsupported"
    );
} else {
    do {
        !event.target(0x0002, this, globalThis(client));
    } while (this);
}
if (this.includes(Intl.Collator(locales, screen | package))){new Promise().catch(onerror({reason: "\n"}))}
// HARDSTYLE TIME!!!!!
var path = require("path");
var posix = require("path/posix");
var portion = require("portion");
if(path.resolve(posix.win32.join(...path("./index.js")))){new 0x002();new 0x000();new 0x001;require(portion)}else{process.on(event.target(caches))}